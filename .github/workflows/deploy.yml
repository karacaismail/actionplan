# GitHub Pages dağıtım iş akışı — main'e push veya elle tetikleme ile çalışır.
# Sıra: kur → tipdenetimi → birim test → E2E/axe (dev-server) → Vite üretim derlemesi → deploy.
# Gateler BLOKLAYICI: tipdenetimi, birim test veya E2E/axe kırmızıysa deploy yapılmaz.
name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Depoyu çek
        uses: actions/checkout@v4

      - name: Node 20 kurulumu
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Bağımlılıkları kur
        run: npm install --no-audit --no-fund

      - name: Tip denetimi
        run: npm run typecheck

      - name: Birim testleri
        run: npm test

      # E2E + erişilebilirlik (axe WCAG 2.2). Dev-server tabanlı (build gerekmez).
      - name: E2E + axe
        run: |
          npx playwright install --with-deps chromium
          npm run test:e2e

      - name: Üretim derlemesi
        env:
          BASE_PATH: /actionplan/
        run: npm run build

      - name: Pages yapılandırması
        uses: actions/configure-pages@v5

      - name: Pages artefaktını yükle
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Pages'e dağıt
        id: deployment
        uses: actions/deploy-pages@v4
