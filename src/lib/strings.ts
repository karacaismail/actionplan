import strings from "@/data/strings.json";

/**
 * TR arayüz metinleri — TEK kaynak `src/data/strings.json`.
 * İçerik JSON'da tutulur; kodda gömülü UI metni yoktur (kullanıcı kuralı).
 */
export const t = strings.ui;
export const STR = strings;
