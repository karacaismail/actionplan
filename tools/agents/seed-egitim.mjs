#!/usr/bin/env node
/** ARCHIVED-LEGACY-MUTATOR — FAIL-CLOSED. Canonical egitim regeneration is disabled. */

console.error(
  [
    "ARCHIVED-LEGACY-MUTATOR — FAIL-CLOSED",
    "Authority: Codex → PM → uzman ajanlar → Claude workers/slaves",
    "AI access: read-only-audit",
    "Executor: human-developer-only",
    "seed-egitim cannot write canonical nodes; use source-owned materializers and reviewed migrations.",
  ].join("\n"),
);
process.exit(2);
