# Gap + Unknown-Unknowns Analizi — Master Index (2026-07-02)

- Üretim yöntemi: çok-ajan paralel okuma (8 ajan), tek elden sentez.
- Doğrulama: her sayı ve iddia koda + veriye karşı çalıştırılarak teyit edildi (kapılar canlı koşuldu, `public/data/nodes.json` yeniden hesaplandı).
- Kapsam: `actionplan` reposu (planlama/sözleşme/WBS katmanı). Commit `04cce25`, 467 WBS düğümü.
- Durum: **tarihsel snapshot / güncel notlarla okunur**. Bu dosya ve kardeşleri, 2026-07-02 anlık denetiminde `elestiri-00/01/02/03-2026-07-01` ve `kernel-dokuman-gap-2026-07-01` dosyalarını ilgili başlıklarda **günceller (supersedes)**. 2026-07-08 itibarıyla bazı kapılar, workspace manifesti ve handoff yüzeyi eklenmiştir; bu rapordaki eski "yok" satırları §9 ve güncel notlarla birlikte okunur.

Kardeş dosyalar:
- `gap-2026-07-02-01-kernel.md` — Kernel katmanı.
- `gap-2026-07-02-02-archetype.md` — Archetype katmanı.
- `gap-2026-07-02-03-surface.md` — Surface katmanı.
- `drafts/` — bu analizin önerdiği artefaktların tarihsel taslak staging alanı. 2026-07-08 itibarıyla ilgili yönergeler `docs/` altına, kapılar `tools/agents/` altına terfi etmiştir; `docs/drafts/` source of truth değildir.

Güncel kullanım notu (2026-07-08): implementation workspace için kanonik kaynaklar `docs/implementation-workspace-manifest.md`, `src/data/workspace-manifest.json`, `docs/task-to-code-contract.md`, `docs/ready-for-dev-gate.md` ve `docs/waterfall-developer-handoff.md` dosyalarıdır. Yerel implementation checkout'u `/Users/karaca/DEV/mimari/platform`, branch `master`, remote yok olarak belgelenmiştir. Bu repo ürün kodu değil, plan/sözleşme/handoff deposudur.

Terim kısaltmaları (ilk geçtikleri yer): WBS = iş kırılım ağacı (görev düğümleri). CI = kod gönderilince testleri/kapıları otomatik çalıştıran süreç. Kapı (gate) = merge/deploy'u bloklayan otomatik denetim. PR = ana koda girmeden önce incelenen değişiklik isteği. PDP = yetki karar noktası (Policy Decision Point). Outbox = veritabanı işlemiyle aynı transaction'da yazılan olay kaydı (mesaj kaybını önler). Idempotent = aynı isteğin iki kez işlenmesi tek etki üretir.

Şiddet ölçeği (tüm dosyalarda ortak):
- **P0** — bu eksik, hedef ürünleri *doğru/eksiksiz* kurmayı engeller. Kurucu boşluk.
- **P1** — ürün kurulur ama *enterprise seviyesine* çıkmasını engeller.
- **P2** — cila, tutarlılık, ileriye dönük risk.

---

## 0. Tek cümlelik hüküm

Üç katmanın (kernel / archetype / surface) **yönerge setleri gerçekten vardır ve bir spec aşaması için olağandışı zengindir** — özellikle kernel. 2026-07-02 snapshot'ında sistem iki ayrı boşlukla maluldü: (1) **aktivasyon boşluğu** — yazılmış sözleşmelerin yarısı ya boş-yeşil ya içi-boş kapılarla "korunuyordu" ve bazı kapılar dokümanda "CI'da çalışıyor" denmesine rağmen henüz yazılmamıştı; (2) **içerik boşluğu** — archetype ve surface katmanlarında hedef ürünler için zorunlu birkaç domain metamodeli ve surface tipi eksikti. 2026-07-08 itibarıyla event/relation/state/secrets kapıları ve handoff/workspace manifesti eklendi; kalan temel gerçek değişmedi: **bu repoda platform kodu yoktur; tüm "kernel sözleşmesi" kapıları kodu değil, plan/sözleşme/düzyazı yüzeyini doğrular**.

---

## 1. Bu analiz neyi kapsar, neyi kapsamaz

Kapsar: `actionplan` reposundaki üç katmanın yönergeleri, sözleşmeleri, CI kapıları ve 467 WBS düğümünün durumu. Codex raporunun güncel durumla uzlaştırılması. Katmanlar-arası bilinmeyen-bilinmeyenler. 18 hedef ürün portföyüne uyum.

Kapsamaz: gerçek platform/kernel runtime kod denetimi. 2026-07-02 oturumunda `atonota/kernel` ve eski platform yolu okunamadığı için kod kalitesi ölçülmedi. 2026-07-08 itibarıyla yerel implementation checkout'u `/Users/karaca/DEV/mimari/platform` olarak belgelenmiştir; bu rapor yine de **planlama/sözleşme katmanını** ölçer, platform kodunu audit etmiş sayılmaz (bkz. §7 U1).

---

## 2. Codex raporu ile uzlaştırma

Codex'in ana tezi ("sözleşme var ama zorlanmıyor") **doğru yönde**. Ancak Codex'in somut kapı iddialarının 3'ü artık **eskimiş** (o kapılar yazılmış ve CI'a bağlanmış), 2'si **hâlâ doğru** (o kapılar gerçekten yok). Aşağıdaki tablo tek tek doğrulanmıştır.

Aşağıdaki tablo, Codex'in "eksik" dediği her kapının bugünkü gerçek durumunu gösterir.

| Codex iddiası | Bugünkü gerçek | Kaynak |
|---|---|---|
| `check-execution-contract.mjs` yok | **VAR + CI'da** | `tools/agents/check-execution-contract.mjs`; `deploy.yml:137` |
| `check-atomic-types.mjs` yok | **VAR + CI'da** | `tools/agents/check-atomic-types.mjs`; `deploy.yml:131` |
| `check-fragments.mjs` yok | **VAR + CI'da** | `tools/agents/check-fragments.mjs`; `deploy.yml:134` |
| `check-event-semantics.mjs` yok | **ARTIK VAR + CI'da**. 2026-07-02 snapshot'ında yoktu; 2026-07-08 itibarıyla aktivasyon boşluğu kapandı. | `tools/agents/check-event-semantics.mjs`; `package.json` `qa:flow`; `.github/workflows/deploy.yml` |
| `check-archetype-relation.mjs` yok | **ARTIK VAR + CI'da**. 2026-07-02 snapshot'ında yoktu; 2026-07-08 itibarıyla aktivasyon boşluğu kapandı. | `tools/agents/check-archetype-relation.mjs`; `package.json` `qa:flow`; `.github/workflows/deploy.yml` |

Sonuç: 2026-07-02 snapshot'ındaki "eksik kapı" iddiaları 2026-07-08 itibarıyla büyük ölçüde kapanmıştır. Ama asıl uyarı hâlâ geçerli: var olan kapıların bir kısmı gerçek platform kodu değil, plan/sözleşme yüzeyini denetler (bkz. §6). Yani gap artık "kapı dosyası yok" değil, "kapı kapsamı plan katmanıyla sınırlı" problemidir.

---

## 3. Doğrulanmış güncel durum (sayılar)

Aşağıdaki sayılar `public/data/nodes.json` üzerinde yeniden hesaplandı; Codex'in verdiği rakamlarla birebir tutuyor.

| Ölçüt | Değer | Not |
|---|---|---|
| Toplam düğüm | 467 | — |
| status = backlog | 467/467 | Hiçbiri ilerlememiş |
| phase = requirements | 467/467 | Hiçbiri development'a girmemiş |
| Evidence (kanıt) dolu | 7/467 | 460 boş |
| Traceability (kod izi) objesi olan | 20/467 | 447 yok |
| standardRef taşıyan | 7/467 | 460 boş |
| status = done | 0 | — |
| phase = development | 0 | — |
| Semantik kayma (traceability "in-progress/scaffolded" ama status "backlog") | 7 düğüm | Tek bir CRM pilot zinciri |
| Dimension içeriği AI (swarm) üretimi | %91.6 | İnsan onayı zaman damgası (`lastReviewed`) **0** slotta |

Buradan çıkan iki gerçek: (1) sistem **yürütme öncesi planlama** halindedir, üretime hazır değildir; "CI yeşil" = "plan verisi kendi kapılarını ihlal etmiyor" demektir, "ürün hazır" demek değildir. (2) İçeriğin neredeyse tamamı yapay zekâ tarafından üretilmiş ve **hiçbiri insan tarafından onaylanmış olarak işaretlenmemiştir** — `status=filled` "AI doldurdu" demektir, "insan doğruladı" demek değildir.

---

## 4. Kullanıcının sorusu: üç katmanın yönerge karşılıkları var mı?

Doğrudan cevap: **Evet, üçünün de yönerge seti var.** Ama olgunluk ve en büyük açık katmanlara göre farklıdır. Ayrıntı kardeş dosyalarda.

| Katman | Yönerge seti var mı? | Olgunluk | En büyük tek açık |
|---|---|---|---|
| Kernel | Var (v1 çekirdek 10 + v2 15 primitif + v3 10 `k-*` yönergesi) | Yüksek; ama v3'ün tamamı "taslak", kilitli değil | `k-kms` (sır/anahtar yönetimi) **hiç yok** ama başka primitifler ona bağımlı |
| Archetype | Var (workflow + ledger/order/inventory/messaging yönergeleri dahil; storage-canonical KİLİTLİ) | Orta; runtime kanıtı ve fixture kapsamı eksik | Workflow/ledger/order/inventory/messaging sözleşmeleri artık var; kalan risk platform implementation evidence + payroll/BOM/routing gibi tam yönergelerdir |
| Surface | Var (spec + v2 + 4 addendum) | Düşük–orta; şema ile doküman ayrışmış | Şemada 8 tip, dokümanda 27 tip; **üç panel katmanı** (developer / SaaS süper-admin / son müşteri) şemada birinci sınıf değil |

---

## 5. Ana teşhis: iki boşluk + bir yapısal gerçek

**Boşluk A — Aktivasyon (spec yazıldı, zorlanmıyor).** 18 CI kapısından kabaca 9'u gerçek/dolu veriye karşı çalışıyor; 9'u ya **boş-yeşil** (denetleyecek kayıt olmadığı için geçiyor — örn. 0 development düğümü olduğundan `check-ready-for-dev` hiçbir şeyi sınamıyor) ya da **içi-boş** (dosya adı/altstring arıyor, gerçek kod davranışını değil — örn. `check-execution-contract` 7 dosyanın *varlığına* bakar, 7-eksenli çalıştırma zarfını doğrulamaz). Ayrıntı §6.

**Boşluk B — İçerik/runtime kanıtı.** Kernel primitif listesi neredeyse tam; 2026-07-02'de archetype ve surface'te ledger, order/line-item, inventory/stok, messaging/thread ve Workflow yönergesi eksikti. 2026-07-08 itibarıyla bu sözleşmelerin çoğu yazıldı; kalan risk payroll, BOM/routing, bazı surface tipleri ve yazılmış sözleşmelerin platform implementation/evidence paketine bağlanmasıdır.

**Yapısal gerçek (Boşluk C değil, zemin).** Bu repo **frontend-only bir SPA'dır (JSON'u veritabanı gibi kullanan planlama aracı)**. `docs/repo-reality-audit.md` bunu açıkça söyler. Sonuç: `check-core-contract`, `check-execution-contract`, `check-atomic-types`, `check-fragments` gibi tüm "kernel sözleşmesi" kapıları **markdown düzyazısında veya şema string'inde** desen arar; gerçek FastAPI/SQLAlchemy kodunu denetlemez, çünkü o kod bu repoda yoktur. Bu, "AI önerir → insan onaylar → motor uygular" güvence zincirinin makine tarafında **hiç kapatılmadığı** anlamına gelir: zincir düzyazıdır, kod değil.

---

## 6. CI kapıları: gerçek / boş-yeşil / içi-boş / eksik

Bu tablo, "spec → zorlama" tezinin omurgasıdır. Her kapı canlı çalıştırıldı. "Boş-yeşil" = geçiyor çünkü denetleyecek kayıt yok. "İçi-boş" = geçiyor çünkü yalnızca dosya/altstring varlığına bakıyor, dokümanın vaat ettiği asıl davranışı doğrulamıyor.

Aşağıdaki tablo hangi kapının gerçekten koruma sağladığını, hangisinin sadece yeşil göründüğünü ayırır.

| Kapı | Durum | Gerçek tetikleyici / neden zayıf |
|---|---|---|
| check-content | Gerçek | 467 düğümün içeriğini şablon-imzasına karşı tarar |
| check-data-quality | Gerçek | owner %100, dangling ref yok, DAG döngü yok — ama faz-sırası zorlamaz (doküman zorladığını iddia etse de) |
| check-tech-profile | Gerçek | Yasak kütüphane taraması (`src/**`), headless kilidi |
| check-dependency-policy | Gerçek | Yasak paket + güvensiz sürüm (`package.json`) |
| check-ui-standards | Gerçek | Emoji/styled-kit taraması (`src/**`) |
| check-short-code | Gerçek | Dosya satır tavanı (`src/**`) |
| check-ruleset / check-surface | Gerçek | ECA/surface kataloğu tutarlılığı + çapraz-referans (surface↔ruleset gerçekten doğrulanıyor) |
| check-scale-invariant | Dar ama gerçek | Yalnız 4 etiketli düğümü sınar (küçük payda) |
| check-standards-coverage | **Boş-yeşil (tasarımca)** | Sadece *boş olmayan* ref'lerin çözüldüğünü sınar; 460/467 düğüm boş → hiç denetlenmez. Kapsam eşiği YOK |
| check-waivers | Boş-yeşil | Tüm repoda 1 waiver var |
| check-dimension-applicability | Kısmen | 40/467 düğüm haritayı kullanıyor |
| check-ready-for-dev | **Boş-yeşil** | 0 development düğümü → hiçbir şeyi sınamıyor. Ayrıca dokümanı 10 alan söz veriyor, kod 3 alan denetliyor |
| check-execution-readiness (Kural 1-2) | **Boş-yeşil** | 0 done + 0 dev+ düğüm; yalnız Kural 3 (13 platform düğümü) çalışıyor, o da yüzeysel |
| check-core-contract | **İçi-boş** | Markdown'da 5 primitif için regex arar; gerçek kodu değil |
| check-execution-contract | **İçi-boş** | 7 dosyanın varlığı + tek "ADR-A5" altstring'i; çalıştırma zarfını doğrulamaz |
| check-atomic-types | **İçi-boş** | 17 tip-adı string'i tek şema dosyasında arar; dokümanın vaat ettiği "13 sözleşme boyutu" denetimini yapmaz |
| check-fragments | **İçi-boş** | Şemada 3 alan-adı altstring'i arar; fragment örneğini doğrulamaz |
| check-event-semantics | Dar ama gerçek | `qa:flow` içinde çalışır; kanıtsız `exactly-once` iddiasını bloklar, eksik beyanlarda WARN ratchet üretir |
| check-archetype-relation | Dar ama gerçek | `qa:flow` içinde çalışır; geçersiz relation kind sert-fail, sarkan hedef WARN üretir |
| check-secrets / gitleaks | Hafif ama gerçek | `tools/agents/check-secrets.mjs` desen-tabanlı kapıdır; git geçmişi/entropi taraması yapmaz, tam gitleaks yerine hafif bloklayıcıdır |

Güncel hüküm: 2026-07-02 yanlış-güven bulgusu kapı dosyası/CI bağlantısı açısından kapanmıştır. Kalan risk, bu kapıların platform runtime davranışını değil plan/sözleşme içeriğini denetlemesidir; code-start kanıtı için `traceability.repoPath`, `traceability.testCommand`, Evidence Patch ve implementation workspace manifesti birlikte aranır.

---

## 7. Katmanlar-arası unknown-unknowns

Bunlar hiçbir mevcut dokümanda risk olarak izlenmeyen, ama 18 ürünü tek kernel'den üretme hedefini doğrudan tehdit eden bilinmeyenlerdir.

**U1 — Platform kodu bu repoda yok; kapılar bu repoda platform runtime'ı doğrulamıyor.** (Yapısal, §5.) Bu repoda `backend/` yok. "Kernel sözleşmesi" kapıları plan/sözleşme/düzyazı doğrular. 2026-07-08 itibarıyla yerel implementation checkout'u `/Users/karaca/DEV/mimari/platform`, branch `master`, remote yok olarak manifestte kayıtlıdır; ancak bu rapor o checkout'u kod kalitesi açısından audit etmiş sayılmaz. Öneri: ilk gerçek `development` düğümü için `repoPath` + `testCommand` + PR/CI/deploy/rollback/audit kanıtı taşıyan golden evidence pack üret; kapıların code-start seviyesinde yeterliliğini orada ölç.

**U2 — Vibecoding + 3 kişilik ekip + insan onayı çelişkisi.** Her yönerge "AI önerir → insan onaylar → motor uygular" der. Ama bu, her AI taslağını okuyup anlayan bir insan varsayar. Jr seviye ekiple, 18 ürün boyunca günde onlarca AI taslağı onaylanırken **onay yorgunluğu / göstermelik onay (rubber-stamp)** riski hiçbir yerde izlenmiyor. Üstelik onay kapısı tek bir kütüphane değil, 10+ dokümanda tekrarlanan bir *desendir*; 18 ekipten biri onu eksik uygularsa fark edilmez. Öneri: onay-kapısını tek, paylaşılan, test edilmiş bir motora indir; desen olarak bırakma.

**U3 — Atomik tip borcu 18 ürüne aynı anda yayılıyor.** `Address`/değer-nesnesi sınırı (atom mı, mini-archetype mi, opak JSON mu?) karara bağlanmamış. Üç jr ekip HRMS/PIM/CLM'i paralel kurarken bunu üç farklı biçimde çözerse ürünler sessizce ıraksar ve kernel seviyesinde birleşemez hale gelir. Görünmezdir; iki ekibin Address modeli çarpışana kadar ortaya çıkmaz.

**U4 — Portföy sayısı belirsiz.** README "28 app / hedef 50+" der; başka bir doküman "16 app"; kullanıcı "18"; veri yalnız ~10 gerçek ürün app'i + gerisi altyapı gösterir. `app-egitim` ve `app-edu` neredeyse çift kayıt. **JOB-BOARD gerçekten yok**: `s-ats` içeri dönük işe-alım aracıdır, halka açık ilan pazaryeri (Indeed/LinkedIn/sahibinden) değildir. Kararı insan vermeli: kanonik portföy sayısı ve JOB-BOARD için yeni düğüm.

**U5 — Shared hosting gereksinimi mimariyle çelişiyor.** Kullanıcı "WordPress gibi shared hosting'de kurulup çalışsın" istiyor. Ama seçilen yığın (FastAPI + PostgreSQL + Docker) klasik cPanel shared hosting'de (root'suz, Docker'sız, PHP/MySQL tek süreç) **çalışamaz**. Repo yalnızca "tek-VPS Docker Compose" senaryosunu karşılar; bu, "shared hosting" iddiasından dar bir alt kümedir. Güvenli: iddiayı "tek-VPS/self-host" olarak düzelt. Riskli: pazarlama "shared hosting" derken teknik gerçek "VPS" ise beklenti kırılır. Enterprise-doğru: hedef deploy topolojilerini (tek-VPS → çok-replika K8s) ve *aralarındaki geçiş yolunu* tek bir runbook'ta netleştir.

**U6 — Dokümanlar günler içinde eskiyor.** Düğüm sayısı tarihsel dokümanlarda farklı snapshot değerleriyle dağılabiliyor; güncel canonical sayı `src/data/generated/meta.json` ve `public/data/nodes.json` üzerinden 467'dir. `AGENTS.md:87` "WCAG 2.2 AAA hedefi" derken `surface.ts:35` tabanı "2.2-AA" yapmış; veri örnekleri ise "2.2-AAA"ya sabit; kapı hiçbir değeri denetlemiyor. Öneri: her rapora "şu commit/tarihe karşı üretildi" başlığı koy; kanonik sayıyı tek yerde tut (single source of truth).

**U7 — Kapı kapsamı ile yetenek kapsamı ters.** 13+ gerçek kapı *yapısal uyumu* zorlar (düğümün şekli doğru mu). Hiçbiri *ürün yeteneği tamlığını* zorlamaz (MRP'de gerçekten BOM var mı). Bir jr geliştirici tüm kapılardan geçip BOM'suz bir MRP gönderebilir. Bu uyumsuzluk QA/UAT'ye kadar görünmez.

---

## 8. Öncelik sırası (aktivasyon + içerik backlog)

Sıralama, kullanıcı tercihindeki yazılım sırasına uyar: önce test/kapı, sonra veri modeli, sonra geliştirme. Aktör her maddede açıktır.

**P0 — önce bunlar (yanlış-güveni ve kurucu boşlukları kapatır):** [Not: 1-6 bu turda büyük ölçüde uygulandı — bkz. §9. 7 insan kararı olduğundan zorlanmadı.]
1. `check-event-semantics` + `check-archetype-relation` kapılarını YAZ ve CI'a bağla. **Durum 2026-07-08: tamamlandı.**
2. `check-secrets` (hafif sır deseni kapısı) ekle. **Durum 2026-07-08: tamamlandı; tam git-geçmişi/entropi gitleaks taraması ayrı hardening işidir.**
3. Semantik kayma kapısı: `status + phase + traceability.implementationStatus` çeliştiğinde kırmızı (7 düğüm bugün çelişik).
4. Archetype metamodelleri: Workflow, ledger, order/line-item, inventory/stok ve messaging/thread yönergeleri yazıldı. Kalan P0/P1 iş, bunları implementation evidence ve fixture/conformance kapsamına bağlamak; payroll ve BOM/routing tam yönergelerini ayrıca tamamlamak.
5. `k-kms` primitifini yaz (başka primitifler ona bağımlı).
6. Surface: üç panel katmanını (developer / SaaS süper-admin / son müşteri) şemada birinci sınıf alan yap; navigasyon/menü modelini ekle; checkout tipini ekle.
7. JOB-BOARD için gerçek düğüm; `app-edu`/`app-egitim` çift kaydını birleştir. (Aktör: insan kararı.)

**P1 — enterprise seviyesi:**
8. standardRef kapsam mandalı (ratchet): önce app, sonra module, sonra archetype seviyesinde zorunlu ref seti. Bugün kapsam eşiği yok.
9. Eksik standartlar: FinOps/metering, olay yönetimi/on-call, müşteri SLA, uyum-sertifikası hedefi (SOC2/ISO27001), içerik moderasyonu, e-posta teslim edilebilirliği.
10. Kernel eksikleri: metering/kullanım-bazlı faturalama, tenant yaşam döngüsü (provision/suspend/offboard/export/delete), payment/PSP adaptörü, gerçek-zamanlı/WebSocket yayını, medya transcode/streaming, WebRTC/Jitsi sinyali, coğrafi (PostGIS) primitif, e-posta taşıma (SMTP/IMAP), gelen webhook, API sürümleme, DR/backup/PITR.
11. Surface DOC-ONLY tiplerini şemaya + kapıya taşı (e-posta istemcisi, takvim, dosya gezgini, proje Gantt'ı, canlı harita, feed, sohbet).

**P2 — cila/tutarlılık:**
12. Düğüm sayısı tek kaynağa; her rapora commit/tarih başlığı.
13. AA/AAA ayrımını netleştir: `AGENTS.md`, `surface.ts` ve surface verisi aynı şeyi söylesin; kapı değeri denetlesin.
14. `aday` cluster'ının "mezuniyet" kriterini yaz (EMAIL/TEAMS/IBYS orada bekliyor).

---

## 9. Bu turda uygulanan (yazıldı + CI'a bağlandı)

İkinci tur (2026-07-02, "uygula"): analizden çıkan boşluklar fiilen kapatıldı. Ayrıntılı günlük: `gap-2026-07-02-05-uygulama-raporu.md`.

- 7 eksik yönerge TAM yazıldı (`docs/`): workflow, k-kms, archetype-ledger, archetype-order-line-item, archetype-inventory-stock, archetype-messaging-thread, panel-tier. Durum: "taslak — insan onayı ile kilitlenecek" (repo'nun kilitli/taslak ayrımına uygun; kilitleme insan kararıdır).
- 4 kapı gerçek kapıya dönüştürüldü ve `.github/workflows/deploy.yml` build job'ına bağlandı: check-event-semantics, check-archetype-relation, check-state-machine-consistency, check-secrets. Böylece event-replay ve tree-relation dokümanlarının "CI'da çalışıyor" dediği kapılar artık gerçekten çalışıyor (yanlış-güven kapandı).
- Kapı tasarımı kademelidir (ratchet): sert-fail yalnız imkansız/yasak durumda (status=done iken kod not-started; geçersiz relation.kind; exactly-once iddiası; gerçek sır deseni). Eksik-ama-beklenen durumlar WARN olarak raporlanır (build'i kırmaz; insan içerik olgunlaştıkça sertleşir). Bu, "boş-yeşil"i büyütmeden yanlış-güveni kapatır.
- `order.json` ArcheType fixture eklendi ve `tests/archetype.test.ts`'e dahil edilerek Zod şemasına karşı doğrulandı; `customer → order` sarkan referansı çözüldü.
- Doğrulama: `biome check .` + `tsc` + tüm CI kapıları (mevcut + 4 yeni) + vitest yeşil; yalnız yeni/ek dosya.

İnsan kararı bekleyen (bu turda ZORLANMADI): pilot düğüm statülerini ilerletme; A-serisi ADR'leri kilitleme; portföy sayısı + JOB-BOARD düğümü; yeni primitifler için WBS düğümü üretimi (seed pipeline); `docs/drafts/` klasörünün silinmesi (kullanıcı reddetti; tarihsel korunur).

---

## 10. Kanıt ve kaynak

Tüm iddialar dosya + satır referanslıdır; ayrıntı kardeş dosyalardadır. Bu analiz 2026-07-02 snapshot'ında platform/kernel runtime kodunu görmedi; 2026-07-08 itibarıyla implementation workspace ayrıca manifestte belgelenmiştir. Bulgular planlama/sözleşme katmanına aittir.
